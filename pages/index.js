const index = () => {
    return (
        <code style={{
            display: `block`,
            width: `75vw`,
            margin: `20px auto`,
            fontSize: `1.8em`,
        }}>
            {
                `react, next js , redux/toolkit template for test work `}<br></br>{
                `${new Date()}`
            }
        </code >
    );
};

export default index;